language: python
env: D="http://vivo.school.edu/individual/"
python:
  - 2.6
  - 2.7
install:
  - pip install -r requirements.txt --use-mirrors
# command to run tests, e.g. python setup.py test
script:
  - py.test